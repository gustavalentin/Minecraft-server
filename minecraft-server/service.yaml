apiVersion: v1
kind: Service
metadata:
  name: minecraft-service
  labels:
    app: minecraft
spec:
  type: NodePort
  ports:
  - port: 25565
    targetPort: 25565
    nodePort: 30001 # Cambia este puerto si es necesario
  selector:
    app: minecraft
